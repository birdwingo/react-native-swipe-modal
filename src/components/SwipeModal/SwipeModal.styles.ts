import { StyleSheet } from 'react-native';

export default StyleSheet.create( {
  barContainer: {
    width: '100%',
    height: 50,
    alignItems: 'center',
    justifyContent: 'center',
  },
  bar: {
    width: 34,
    height: 5,
    borderRadius: 10,
  },
} );
